#include <stdio.h>
int main(){
    int area, comprimento, largura;
    comprimento = 2;
    largura = 3;
    area = (comprimento *largura);
    printf("Comprimento: %1.i; Largura: %1.i; Área: %1.i.\n", comprimento, largura, area);
    return 0;
}